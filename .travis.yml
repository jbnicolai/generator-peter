language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-peter' ]; then cd .. && eval "mv $currentfolder generator-peter" && cd generator-peter; fi

